export default function({store,req}){
  // let {token_expires} = store.state;
  // if(!token_expires || Number(token_expires)< Date.now()) store.dispatch("loadUserInfo")
  // console.log(req.headers.cookie)
}
